import express from 'express';

const router = express.Router();

router.get('/', );

router.post('/');

router.get('/:id');

router.patch('/:id');

router.delete('/:id');

export const SpecimenRoutes = router;
